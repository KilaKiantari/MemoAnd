<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shela.memoand">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:label="NOEGAS">
        </activity>
        <activity android:name=".CreateNote"
            android:label="Create Tugas">
        </activity>
        <activity android:name=".View_Note"
            android:label="Detil Tugas">
        </activity>
        <activity android:name=".ViewAll"
            android:label="Prioritas Tugas">
        </activity>
        <activity android:name=".Edit_Note"
            android:label="Edit Tugas">
        </activity>
        <activity android:name=".Alert"
            android:label="">
        </activity>
        <activity android:name=".AboutUs"
            android:label="About Us">
        </activity>

    </application>

</manifest>